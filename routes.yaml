routes:
  /:
    data: page.home
    template: home

collections:
  /articles/:
    permalink: /{slug}/
    template: index
    filter: 'tag:-hash-ru'
  /ru/:
    permalink: /ru/{slug}/
    template: index-ru
    filter: 'tag:hash-ru'

taxonomies:
  tag: /tag/{slug}/
